{
  "name": "guardette-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run types && wrangler deploy",
    "types": "wrangler types",
    "typecheck": "npm run types && tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.112.0"
  }
}
